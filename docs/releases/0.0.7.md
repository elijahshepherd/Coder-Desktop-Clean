# Coder Desktop - Version 0.0.7

> A clear overview of everything included in this release, written for users, developers, and contributors.

**Release date:** June 3, 2026

**Version:** `0.0.7`

**Status:** Beta

**Platform support:** Windows / macOS / Linux

**Full changelog:** https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

---

# Release Summary

## Overview

Coder Desktop 0.0.7 focuses on Windows PowerShell information cards. This update matters because it improves the parts of the desktop app that users touch while building, testing, updating, or managing local AI-assisted work.

This version continues the local-first direction of Coder Desktop by tightening quality, release consistency, documentation, and day-to-day reliability. Users should expect a more predictable app surface and developers should have a clearer record of what changed in this version.

---

# Major Highlights

> This release includes three major highlights for users, testers, and developers.

## PowerShell Information Cards

### This highlight covers the main Windows PowerShell information cards work completed in this release.

* Added Windows PowerShell activity cards for safe information commands.

* Added command grouping for requests such as processor details, system information, networking, services, running processes, date, hostname, and current user.

* Added a `windows-ps-group` tool request format for provider-driven command use.

* Added renderer state updates from the main process so chat activity can update progressively.

* Added tests for PowerShell command grouping, provider prompt tool context, same-version update refreshes, and tool-result compaction.

---

## Progressive Chat Activity

### This highlight explains the reliability, safety, and user-confidence improvements included in the release.

* Fixed the AI prompt so Coder Desktop no longer tells providers that local command tools are unavailable when safe PowerShell information commands are enabled.

* Fixed chat sending so user messages and tool activity appear while work is running instead of waiting for one final response.

* Fixed update detection so same-version release refreshes can be offered when a release asset is newer than the installed executable.

* Fixed the GitHub Release publishing workflow so it checks out Git LFS assets before uploading release downloads.

* Kept destructive, permission-changing, installation, deletion, shutdown, credential, registry-writing, firewall-changing, and disk-formatting commands out of the automatic PowerShell tool path.

---

## Release Workflow Repair

### This highlight captures the release-readiness work that keeps Coder Desktop easier to ship and maintain.

* Improved chat readability by keeping PowerShell output inside compact custom cards.

* Improved tool safety by allowing only approved read-only information commands through the automatic PowerShell command path.

* Improved release reliability by preparing a real Windows x64 ZIP artifact for version `0.0.7`.

* Prepared version `0.0.7` with a real Windows x64 ZIP download, extension ZIP package, manifest, release notes, and GitHub Release workflow support.

* Changed the provider system prompt into a branded, sectioned Coder Desktop prompt with explicit local tool state.

---

# Technical Improvements

> This release includes the technical changes that make the update reliable.

## PowerShell Command Grouping

This release strengthened powershell command grouping as part of the Windows PowerShell information cards work.

The impact is better reliability, clearer ownership of the affected system, and a release path that is easier to validate before publishing.

---

## Live Renderer State Updates

This release improved live renderer state updates so the app behavior and release artifacts stay aligned.

The impact is better maintainability, more predictable packaging, and clearer expectations for users moving between versions.

---

# Included Changes

> Completed work is grouped by the area it affects.

## Added

### User-Facing Additions

* Added Windows PowerShell activity cards for safe information commands.

* Added command grouping for requests such as processor details, system information, networking, services, running processes, date, hostname, and current user.

* Added a `windows-ps-group` tool request format for provider-driven command use.

* Added renderer state updates from the main process so chat activity can update progressively.

* Added tests for PowerShell command grouping, provider prompt tool context, same-version update refreshes, and tool-result compaction.

### Documentation And Release Additions

* Prepared version `0.0.7` with a real Windows x64 ZIP download, extension ZIP package, manifest, release notes, and GitHub Release workflow support.

* Added version `0.0.7` release notes and download documentation.

* Documented the release in the versioned release notes.

* Kept the changelog aligned with the shipped version.

* Prepared release metadata for users and contributors.

### Developer Additions

* Kept destructive, permission-changing, installation, deletion, shutdown, credential, registry-writing, firewall-changing, and disk-formatting commands out of the automatic PowerShell tool path.

* Kept command execution behind the Windows PowerShell security setting.

* Kept update discovery and asset selection inside the Electron main process.

* Prepared version `0.0.7` with a real Windows x64 ZIP download, extension ZIP package, manifest, release notes, and GitHub Release workflow support.

* Added release context that helps developers review the version.

---

## Improved

### Product Experience

* Improved chat readability by keeping PowerShell output inside compact custom cards.

* Improved tool safety by allowing only approved read-only information commands through the automatic PowerShell command path.

* Improved release reliability by preparing a real Windows x64 ZIP artifact for version `0.0.7`.

* Changed the provider system prompt into a branded, sectioned Coder Desktop prompt with explicit local tool state.

* Changed workspace command execution to run through PowerShell directly so PowerShell cmdlets work correctly.

### Reliability And Maintainability

* Fixed the AI prompt so Coder Desktop no longer tells providers that local command tools are unavailable when safe PowerShell information commands are enabled.

* Fixed chat sending so user messages and tool activity appear while work is running instead of waiting for one final response.

* Fixed update detection so same-version release refreshes can be offered when a release asset is newer than the installed executable.

* Fixed the GitHub Release publishing workflow so it checks out Git LFS assets before uploading release downloads.

* Kept destructive, permission-changing, installation, deletion, shutdown, credential, registry-writing, firewall-changing, and disk-formatting commands out of the automatic PowerShell tool path.

---

## Fixed

### Fixed The AI Prompt So

Fixed the AI prompt so Coder Desktop no longer tells providers that local command tools are unavailable when safe PowerShell information commands are enabled.

### Fixed Chat Sending So User

Fixed chat sending so user messages and tool activity appear while work is running instead of waiting for one final response.

### Fixed Update Detection So Same

Fixed update detection so same-version release refreshes can be offered when a release asset is newer than the installed executable.

### Fixed The GitHub Release Publishing

Fixed the GitHub Release publishing workflow so it checks out Git LFS assets before uploading release downloads.

### Resolved The Main Issue Targeted

Resolved the main issue targeted by this release so the app behaves more predictably.

---

## Optimized

### Release Quality

* Changed the provider system prompt into a branded, sectioned Coder Desktop prompt with explicit local tool state.

* Changed workspace command execution to run through PowerShell directly so PowerShell cmdlets work correctly.

* Changed update notice wording so same-version refreshes do not claim to be a different version.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

### Safety And Packaging

* Kept destructive, permission-changing, installation, deletion, shutdown, credential, registry-writing, firewall-changing, and disk-formatting commands out of the automatic PowerShell tool path.

* Kept command execution behind the Windows PowerShell security setting.

* Kept update discovery and asset selection inside the Electron main process.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

---

# Compatibility

| Component | Status |
| --------- | ------ |
| Windows | Supported and tested for this release. |
| macOS | Supported through packaged downloads or build workflow where available. |
| Linux | Supported through source and packaging configuration where available. |

---

# Developer Notes

## Internal Changes

* Changed the provider system prompt into a branded, sectioned Coder Desktop prompt with explicit local tool state.

* Changed workspace command execution to run through PowerShell directly so PowerShell cmdlets work correctly.

* Changed update notice wording so same-version refreshes do not claim to be a different version.

* Changed security settings to include a custom Windows PowerShell permission switch.

* Kept destructive, permission-changing, installation, deletion, shutdown, credential, registry-writing, firewall-changing, and disk-formatting commands out of the automatic PowerShell tool path.

---

# Upgrade Guidance

## Before Updating

* Close any running Coder Desktop windows before installing the new version.

* Keep a copy of important project work in source control or another backup before changing local tooling.

* Use the matching GitHub Release page or versioned download folder for the release artifact.

## After Updating

* Open Coder Desktop and confirm the app reports the expected version.

* Check provider settings, workspace access, and security controls if this release changed those areas.

* Run the workflow that mattered for the update, such as chat, local tools, settings, updates, or packaging.

---

# Closing Notes

Coder Desktop 0.0.7 improves Windows PowerShell information cards while keeping the app grounded in a calm local-first desktop workflow. The goal is to make the product more predictable without burying users under unnecessary process or interface noise.

This release also improves the public record of the project. Users, testers, and contributors should be able to understand what changed, why it changed, and how to verify the result from one consistent release note.

Future releases should continue using this structure so every version has the same level of clarity, upgrade guidance, compatibility notes, and technical context.

---

# Resources

* Changelog: https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

* Repository: https://github.com/elijahshepherd/Coder-Desktop
