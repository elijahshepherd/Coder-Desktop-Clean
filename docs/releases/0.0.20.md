# Coder Desktop - Version 0.0.20

> A clear overview of everything included in this release, written for users, developers, and contributors.

**Release date:** June 5, 2026

**Version:** `0.0.20`

**Status:** Patch

**Platform support:** Windows / macOS / Linux

**Full changelog:** https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

---

# Release Summary

## Overview

Coder Desktop 0.0.20 focuses on updater availability, prompt privacy, and image retry repair. This update matters because it improves the parts of the desktop app that users touch while building, testing, updating, or managing local AI-assisted work.

This version continues the local-first direction of Coder Desktop by tightening quality, release consistency, documentation, and day-to-day reliability. Users should expect a more predictable app surface and developers should have a clearer record of what changed in this version.

---

# Major Highlights

> This release includes three major highlights for users, testers, and developers.

## Update Availability

### This highlight covers the main updater availability, prompt privacy, and image retry repair work completed in this release.

* Added regression tests for prompt disclosure requests, image retry prompts, image typo detection, and image capability questions.

* Added version `0.0.20` release packaging support.

* Changed image generation cards and fallback behavior so unavailable selected image models can fall back to compatible image models.

* Changed assistant instruction privacy guidance so the model must not invent or reveal internal prompt locations, section names, or exact hidden text.

* Changed image request detection to tolerate common misspellings such as `iameg`, `imgae`, and `iamge`.

---

## Prompt Privacy Guard

### This highlight explains the reliability, safety, and user-confidence improvements included in the release.

* Fixed update availability for users on `0.0.19` by preparing a newer packaged release version for the updater to detect.

* Fixed hidden prompt disclosure handling so requests for exact system or developer instructions are answered safely before reaching a provider.

* Fixed image retry behavior so short follow-ups such as `try again` reuse the latest image prompt in the current chat.

* Fixed image request detection so capability questions such as `can you create images` do not accidentally start generation.

* Kept hidden system prompts, developer instructions, tool routing rules, and internal prompt sections out of user-visible chat responses.

---

## Image Retry Repair

### This highlight captures the release-readiness work that keeps Coder Desktop easier to ship and maintain.

* Improved updater clarity by shipping the current fixes as a real next version instead of relying on source-only commits.

* Improved image generation recovery when a provider rejects a selected image model.

* Improved chat behavior around image follow-ups so users can retry without rewriting the full prompt.

* Prepared version `0.0.20` as a patch release with updater availability, prompt privacy, image retry, and image generation fallback improvements.

* Changed image generation cards and fallback behavior so unavailable selected image models can fall back to compatible image models.

---

# Technical Improvements

> This release includes the technical changes that make the update reliable.

## Updater Release Path

This release strengthened updater release path as part of the updater availability, prompt privacy, and image retry repair work.

The impact is better reliability, clearer ownership of the affected system, and a release path that is easier to validate before publishing.

---

## Prompt Disclosure Guard

This release improved prompt disclosure guard so the app behavior and release artifacts stay aligned.

The impact is better maintainability, more predictable packaging, and clearer expectations for users moving between versions.

---

# Included Changes

> Completed work is grouped by the area it affects.

## Added

### User-Facing Additions

* Added regression tests for prompt disclosure requests, image retry prompts, image typo detection, and image capability questions.

* Added version `0.0.20` release packaging support.

* Focused this release on updater availability, prompt privacy, and image retry repair rather than adding broad new surface area.

* Kept new behavior tied to the existing desktop workspace model.

* Maintained clear documentation for any new user-facing behavior.

### Documentation And Release Additions

* Prepared version `0.0.20` as a patch release with updater availability, prompt privacy, image retry, and image generation fallback improvements.

* Documented the release in the versioned release notes.

* Kept the changelog aligned with the shipped version.

* Prepared release metadata for users and contributors.

* Kept download expectations tied to real versioned artifacts.

### Developer Additions

* Kept hidden system prompts, developer instructions, tool routing rules, and internal prompt sections out of user-visible chat responses.

* Kept prompt privacy enforcement in the main process so protected requests do not need to be sent to a provider.

* Prepared version `0.0.20` as a patch release with updater availability, prompt privacy, image retry, and image generation fallback improvements.

* Added release context that helps developers review the version.

* Kept internal behavior documented close to the changed system.

---

## Improved

### Product Experience

* Improved updater clarity by shipping the current fixes as a real next version instead of relying on source-only commits.

* Improved image generation recovery when a provider rejects a selected image model.

* Improved chat behavior around image follow-ups so users can retry without rewriting the full prompt.

* Changed image generation cards and fallback behavior so unavailable selected image models can fall back to compatible image models.

* Changed assistant instruction privacy guidance so the model must not invent or reveal internal prompt locations, section names, or exact hidden text.

### Reliability And Maintainability

* Fixed update availability for users on `0.0.19` by preparing a newer packaged release version for the updater to detect.

* Fixed hidden prompt disclosure handling so requests for exact system or developer instructions are answered safely before reaching a provider.

* Fixed image retry behavior so short follow-ups such as `try again` reuse the latest image prompt in the current chat.

* Fixed image request detection so capability questions such as `can you create images` do not accidentally start generation.

* Kept hidden system prompts, developer instructions, tool routing rules, and internal prompt sections out of user-visible chat responses.

---

## Fixed

### Fixed Update Availability For Users

Fixed update availability for users on `0.0.19` by preparing a newer packaged release version for the updater to detect.

### Fixed Hidden Prompt Disclosure Handling

Fixed hidden prompt disclosure handling so requests for exact system or developer instructions are answered safely before reaching a provider.

### Fixed Image Retry Behavior So

Fixed image retry behavior so short follow-ups such as `try again` reuse the latest image prompt in the current chat.

### Fixed Image Request Detection So

Fixed image request detection so capability questions such as `can you create images` do not accidentally start generation.

### Resolved The Main Issue Targeted

Resolved the main issue targeted by this release so the app behaves more predictably.

---

## Optimized

### Release Quality

* Changed image generation cards and fallback behavior so unavailable selected image models can fall back to compatible image models.

* Changed assistant instruction privacy guidance so the model must not invent or reveal internal prompt locations, section names, or exact hidden text.

* Changed image request detection to tolerate common misspellings such as `iameg`, `imgae`, and `iamge`.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

### Safety And Packaging

* Kept hidden system prompts, developer instructions, tool routing rules, and internal prompt sections out of user-visible chat responses.

* Kept prompt privacy enforcement in the main process so protected requests do not need to be sent to a provider.

* Prepared version `0.0.20` as a patch release with updater availability, prompt privacy, image retry, and image generation fallback improvements.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

---

# Compatibility

| Component | Status |
| --------- | ------ |
| Windows | Supported and tested for this release. |
| macOS | Supported through packaged downloads or build workflow where available. |
| Linux | Supported through source and packaging configuration where available. |

---

# Developer Notes

## Internal Changes

* Changed image generation cards and fallback behavior so unavailable selected image models can fall back to compatible image models.

* Changed assistant instruction privacy guidance so the model must not invent or reveal internal prompt locations, section names, or exact hidden text.

* Changed image request detection to tolerate common misspellings such as `iameg`, `imgae`, and `iamge`.

* Kept hidden system prompts, developer instructions, tool routing rules, and internal prompt sections out of user-visible chat responses.

* Kept prompt privacy enforcement in the main process so protected requests do not need to be sent to a provider.

---

# Upgrade Guidance

## Before Updating

* Close any running Coder Desktop windows before installing the new version.

* Keep a copy of important project work in source control or another backup before changing local tooling.

* Use the matching GitHub Release page or versioned download folder for the release artifact.

## After Updating

* Open Coder Desktop and confirm the app reports the expected version.

* Check provider settings, workspace access, and security controls if this release changed those areas.

* Run the workflow that mattered for the update, such as chat, local tools, settings, updates, or packaging.

---

# Closing Notes

Coder Desktop 0.0.20 improves updater availability, prompt privacy, and image retry repair while keeping the app grounded in a calm local-first desktop workflow. The goal is to make the product more predictable without burying users under unnecessary process or interface noise.

This release also improves the public record of the project. Users, testers, and contributors should be able to understand what changed, why it changed, and how to verify the result from one consistent release note.

Future releases should continue using this structure so every version has the same level of clarity, upgrade guidance, compatibility notes, and technical context.

---

# Resources

* Changelog: https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

* Repository: https://github.com/elijahshepherd/Coder-Desktop
