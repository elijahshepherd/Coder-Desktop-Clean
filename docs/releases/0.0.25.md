# Coder Desktop - Version 0.0.25

> A clear overview of everything included in this release, written for users, developers, and contributors.

**Release date:** June 5, 2026

**Version:** `0.0.25`

**Status:** Patch

**Platform support:** Windows / macOS / Linux

**Full changelog:** https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

---

# Release Summary

## Overview

Coder Desktop 0.0.25 focuses on same-provider image retry and NVIDIA image reporting. This update matters because it improves the parts of the desktop app that users touch while building, testing, updating, or managing local AI-assisted work.

This version continues the local-first direction of Coder Desktop by tightening quality, release consistency, documentation, and day-to-day reliability. Users should expect a more predictable app surface and developers should have a clearer record of what changed in this version.

---

# Major Highlights

> This release includes three major highlights for users, testers, and developers.

## Same-Provider Image Retry

### This highlight covers the main same-provider image retry and NVIDIA image reporting work completed in this release.

* Added regression tests for active NVIDIA `qwen-image` failures with missing OpenAI keys.

* Added regression tests that prevent OpenAI image endpoints from being called after a selected NVIDIA image model fails.

* Added release packaging support for version `0.0.25`.

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

* Changed assistant image-generation guidance so the model does not claim cross-provider image fallback will happen silently.

---

## NVIDIA Image Report Accuracy

### This highlight explains the reliability, safety, and user-confidence improvements included in the release.

* Fixed image generation routing so a selected NVIDIA image request no longer falls through to OpenAI and reports `gpt-image-1.5` or an OpenAI API-key error.

* Fixed active-provider image requests so `qwen-image` failures remain reported as NVIDIA image model failures.

* Fixed image generation failure descriptions so they say Coder Desktop tried the selected image provider instead of implying a hidden provider switch.

* Kept image failure reports provider-specific so diagnostic issue bodies do not imply the app used a different provider than the selected one.

* Improved image generation report accuracy by keeping the provider and model context aligned with the selected image provider.

---

## Image Regression Coverage

### This highlight captures the release-readiness work that keeps Coder Desktop easier to ship and maintain.

* Improved image generation report accuracy by keeping the provider and model context aligned with the selected image provider.

* Improved image-generation documentation to explain same-provider retry behavior clearly.

* Prepared version `0.0.25` as a patch release with same-provider image retries, NVIDIA image failure regression coverage, clearer image reporting, and updated release packaging.

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

* Changed assistant image-generation guidance so the model does not claim cross-provider image fallback will happen silently.

---

# Technical Improvements

> This release includes the technical changes that make the update reliable.

## Selected Provider Image Routing

This release strengthened selected provider image routing as part of the same-provider image retry and NVIDIA image reporting work.

The impact is better reliability, clearer ownership of the affected system, and a release path that is easier to validate before publishing.

---

## Image Error Context Preservation

This release improved image error context preservation so the app behavior and release artifacts stay aligned.

The impact is better maintainability, more predictable packaging, and clearer expectations for users moving between versions.

---

# Included Changes

> Completed work is grouped by the area it affects.

## Added

### User-Facing Additions

* Added regression tests for active NVIDIA `qwen-image` failures with missing OpenAI keys.

* Added regression tests that prevent OpenAI image endpoints from being called after a selected NVIDIA image model fails.

* Added release packaging support for version `0.0.25`.

* Focused this release on same-provider image retry and NVIDIA image reporting rather than adding broad new surface area.

* Kept new behavior tied to the existing desktop workspace model.

### Documentation And Release Additions

* Prepared version `0.0.25` as a patch release with same-provider image retries, NVIDIA image failure regression coverage, clearer image reporting, and updated release packaging.

* Documented the release in the versioned release notes.

* Kept the changelog aligned with the shipped version.

* Prepared release metadata for users and contributors.

* Kept download expectations tied to real versioned artifacts.

### Developer Additions

* Kept image failure reports provider-specific so diagnostic issue bodies do not imply the app used a different provider than the selected one.

* Prepared version `0.0.25` as a patch release with same-provider image retries, NVIDIA image failure regression coverage, clearer image reporting, and updated release packaging.

* Added release context that helps developers review the version.

* Kept internal behavior documented close to the changed system.

* Maintained clear safety notes for local-first desktop behavior.

---

## Improved

### Product Experience

* Improved image generation report accuracy by keeping the provider and model context aligned with the selected image provider.

* Improved image-generation documentation to explain same-provider retry behavior clearly.

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

* Changed assistant image-generation guidance so the model does not claim cross-provider image fallback will happen silently.

* Improved the same-provider image retry and NVIDIA image reporting experience for daily Coder Desktop use.

### Reliability And Maintainability

* Fixed image generation routing so a selected NVIDIA image request no longer falls through to OpenAI and reports `gpt-image-1.5` or an OpenAI API-key error.

* Fixed active-provider image requests so `qwen-image` failures remain reported as NVIDIA image model failures.

* Fixed image generation failure descriptions so they say Coder Desktop tried the selected image provider instead of implying a hidden provider switch.

* Kept image failure reports provider-specific so diagnostic issue bodies do not imply the app used a different provider than the selected one.

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

---

## Fixed

### Fixed Image Generation Routing So

Fixed image generation routing so a selected NVIDIA image request no longer falls through to OpenAI and reports `gpt-image-1.5` or an OpenAI API-key error.

### Fixed Active Provider Image Requests

Fixed active-provider image requests so `qwen-image` failures remain reported as NVIDIA image model failures.

### Fixed Image Generation Failure Descriptions

Fixed image generation failure descriptions so they say Coder Desktop tried the selected image provider instead of implying a hidden provider switch.

### Resolved The Main Issue Targeted

Resolved the main issue targeted by this release so the app behaves more predictably.

### Reduced Confusing Behavior In The

Reduced confusing behavior in the affected workflow.

---

## Optimized

### Release Quality

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

* Changed assistant image-generation guidance so the model does not claim cross-provider image fallback will happen silently.

* Improved image generation report accuracy by keeping the provider and model context aligned with the selected image provider.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

### Safety And Packaging

* Kept image failure reports provider-specific so diagnostic issue bodies do not imply the app used a different provider than the selected one.

* Prepared version `0.0.25` as a patch release with same-provider image retries, NVIDIA image failure regression coverage, clearer image reporting, and updated release packaging.

* Removed silent cross-provider image fallback from the image generation path.

* Kept the release focused on practical desktop workflow improvements.

* Preserved local-first behavior across provider, workspace, and release paths.

---

# Compatibility

| Component | Status |
| --------- | ------ |
| Windows | Supported and tested for this release. |
| macOS | Supported through packaged downloads or build workflow where available. |
| Linux | Supported through source and packaging configuration where available. |

---

# Developer Notes

## Internal Changes

* Changed image generation retry behavior to stay inside the selected provider and only try compatible models from that provider.

* Changed assistant image-generation guidance so the model does not claim cross-provider image fallback will happen silently.

* Kept image failure reports provider-specific so diagnostic issue bodies do not imply the app used a different provider than the selected one.

* Removed silent cross-provider image fallback from the image generation path.

* Prepared version `0.0.25` as a patch release with same-provider image retries, NVIDIA image failure regression coverage, clearer image reporting, and updated release packaging.

---

# Upgrade Guidance

## Before Updating

* Close any running Coder Desktop windows before installing the new version.

* Keep a copy of important project work in source control or another backup before changing local tooling.

* Use the matching GitHub Release page or versioned download folder for the release artifact.

## After Updating

* Open Coder Desktop and confirm the app reports the expected version.

* Check provider settings, workspace access, and security controls if this release changed those areas.

* Run the workflow that mattered for the update, such as chat, local tools, settings, updates, or packaging.

---

# Closing Notes

Coder Desktop 0.0.25 improves same-provider image retry and NVIDIA image reporting while keeping the app grounded in a calm local-first desktop workflow. The goal is to make the product more predictable without burying users under unnecessary process or interface noise.

This release also improves the public record of the project. Users, testers, and contributors should be able to understand what changed, why it changed, and how to verify the result from one consistent release note.

Future releases should continue using this structure so every version has the same level of clarity, upgrade guidance, compatibility notes, and technical context.

---

# Resources

* Changelog: https://github.com/elijahshepherd/Coder-Desktop/blob/main/CHANGELOG.md

* Repository: https://github.com/elijahshepherd/Coder-Desktop
