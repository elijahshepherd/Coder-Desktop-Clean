Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"

Write-Host "Labels are defined in .github/labels.yml."
Write-Host "Use the sync labels workflow or a GitHub label sync tool to apply them to the repository."
